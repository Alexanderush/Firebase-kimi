{
  "name": "kimi-platform",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@kimi/web",
    "build": "npm run build --workspace=@kimi/web",
    "start": "npm run start --workspace=@kimi/web",
    "lint": "npm run lint --workspaces"
  }
}